# Fix Logs

This directory stores historical maintenance results that are useful for auditability but are not part of the runtime application.

Each subdirectory should use a timestamped, descriptive name and include only the artifacts needed to understand the fix outcome.
