# Optional break-glass alternative to compose-current-key.sh. If both source
# containers will be deleted, copy this to .env, restrict it to the owner
# (chmod 600), and retain the existing provider key. Never commit the populated file.
OPENAI_API_KEY=replace-with-current-open-webui-provider-key
