apiVersion: batch/v1
kind: Job
metadata:
  name: bddk-mcp-bootstrap-v5-0-1
  labels:
    app.kubernetes.io/name: bddk-mcp
    app.kubernetes.io/component: lifecycle
spec:
  backoffLimit: 1
  ttlSecondsAfterFinished: 86400
  template:
    metadata:
      labels:
        app.kubernetes.io/name: bddk-mcp
        app.kubernetes.io/component: lifecycle
    spec:
      restartPolicy: Never
      serviceAccountName: bddk-mcp-ingestion
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: bootstrap
          image: REPLACE_IMAGE_REGISTRY/bddk-mcp@sha256:REPLACE_64_HEX_IMAGE_DIGEST
          imagePullPolicy: IfNotPresent
          args: [".venv/bin/bddk-mcp", "bootstrap", "--reindex-existing"]
          env:
            - name: BDDK_INGESTION_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: bddk-mcp-ingestion-db
                  key: BDDK_INGESTION_DATABASE_URL
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: "1"
              memory: 3Gi
            limits:
              cpu: "4"
              memory: 8Gi
          volumeMounts:
            - name: postgres-ca
              mountPath: /var/run/configmaps/bddk-mcp/postgres
              readOnly: true
            - name: runtime-tmp
              mountPath: /tmp
      volumes:
        - name: postgres-ca
          configMap:
            name: bddk-mcp-postgres-ca
            items:
              - key: ca.crt
                path: ca.crt
        - name: runtime-tmp
          emptyDir:
            sizeLimit: 1Gi
