apiVersion: apps/v1
kind: Deployment
metadata:
  name: bddk-mcp-operator
spec:
  # Durable PostgreSQL records and advisory leases prevent concurrent corpus
  # mutation, but this starter intentionally uses one Recreate-managed
  # operator until bank acceptance tests cover multi-replica failover.
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: bddk-mcp
      app.kubernetes.io/component: operator
  template:
    metadata:
      labels:
        app.kubernetes.io/name: bddk-mcp
        app.kubernetes.io/component: operator
    spec:
      serviceAccountName: bddk-mcp-operator
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 45
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: server
          image: REPLACE_IMAGE_REGISTRY/bddk-mcp@sha256:REPLACE_64_HEX_IMAGE_DIGEST
          imagePullPolicy: IfNotPresent
          args: [".venv/bin/bddk-mcp", "serve", "--profile", "operator"]
          envFrom:
            - configMapRef:
                name: bddk-mcp-operator-config
          env:
            - name: BDDK_OPERATOR_DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: bddk-mcp-operator-db
                  key: BDDK_OPERATOR_DATABASE_URL
            - name: BDDK_TLS_CERT_FILE
              value: /var/run/secrets/bddk-mcp/tls/tls.crt
            - name: BDDK_TLS_KEY_FILE
              value: /var/run/secrets/bddk-mcp/tls/tls.key
          ports:
            - name: https
              containerPort: 8000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health/live
              port: https
              scheme: HTTPS
            initialDelaySeconds: 10
            periodSeconds: 20
            timeoutSeconds: 2
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/ready
              port: https
              scheme: HTTPS
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: "1"
              memory: 3Gi
            limits:
              cpu: "4"
              memory: 8Gi
          volumeMounts:
            - name: postgres-ca
              mountPath: /var/run/configmaps/bddk-mcp/postgres
              readOnly: true
            - name: service-tls
              mountPath: /var/run/secrets/bddk-mcp/tls
              readOnly: true
            - name: runtime-tmp
              mountPath: /tmp
      volumes:
        - name: postgres-ca
          configMap:
            name: bddk-mcp-postgres-ca
            items:
              - key: ca.crt
                path: ca.crt
        - name: service-tls
          secret:
            secretName: bddk-mcp-operator-tls
            defaultMode: 0440
        - name: runtime-tmp
          emptyDir:
            sizeLimit: 512Mi
