[project]
name = "bddk-mcp"
version = "0.1.0"
description = "MCP server for searching BDDK (Turkish Banking Regulation) decisions"
requires-python = ">=3.11,<3.14"
dependencies = [
    "mcp[cli]>=1.0.0",
    "httpx>=0.27",
    "pydantic>=2.0",
    "markitdown[pdf]>=0.1",
    "beautifulsoup4>=4.12",
]
