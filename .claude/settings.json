{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "CMD=$(jq -r '.tool_input.command // \"\"'); case \"$CMD\" in 'git commit'*) { cd \"$(git rev-parse --show-toplevel)\" && uv run ruff check . && uv run ruff format --check .; } || exit 2;; esac",
            "statusMessage": "Running ruff checks before commit"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(echo \"$CLAUDE_TOOL_INPUT\" | jq -r '.file_path // empty') && [ -n \"$FILE\" ] && echo \"$FILE\" | grep -q '\\.py$' && cd \"$(git rev-parse --show-toplevel)\" && uv run ruff check --fix \"$FILE\" 2>/dev/null || true"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$(git rev-parse --show-toplevel)\" && uv sync --dev 2>/dev/null && echo 'Dependencies ready'"
          }
        ]
      }
    ]
  }
}
